export interface Step1Values {
  name: string;
  birthDate: string;
  gender: string;
  telephone: string;
};

export interface Step2Values {
  email: string;
  password: string;
  confirmPassword: string;
}